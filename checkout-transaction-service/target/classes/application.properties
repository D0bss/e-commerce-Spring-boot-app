server.port=8083
spring.application.name=checkout-transaction-service
spring.datasource.url=jdbc:postgresql://localhost:5432/transactiondb
spring.datasource.username=postgres
spring.datasource.password=password
# spring.jpa.hibernate.ddl-auto=update # This was duplicated, using the one below
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# RabbitMQ Configuration
# spring.rabbitmq.host=localhost # This was duplicated, using the one above
# spring.rabbitmq.port=5672 # This was duplicated
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest
# spring.rabbitmq.virtual-host=/ # Optional: if you use a specific virtual host

# Jackson Serialization (Optional: for pretty print JSON in logs/responses during dev)
# spring.jackson.serialization.indent_output=true

# Logging Configuration (example)
logging.level.com.example.checkout=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=voaygemasters@gmail.com
spring.mail.password=ezkk vsxm varp vmpu
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

