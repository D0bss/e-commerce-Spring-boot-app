apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: admin-mongo
spec:
  serviceName: "admin-mongo"  # Must match headless service
  replicas: 1  #FIXME CHANGE
  selector:
    matchLabels:
      app: admin-mongo
  template:
    metadata:
      labels:
        app: admin-mongo
    spec:
      containers:
        - name: admin-mongo
          image: mongo
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              valueFrom:
                secretKeyRef:
                  name: admin-mongo-secret
                  key: MONGO_INITDB_ROOT_USERNAME
            - name: MONGO_INITDB_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: admin-mongo-secret
                  key: MONGO_INITDB_ROOT_PASSWORD
          volumeMounts:
            - name: admin-mongo-persistent-volume
              mountPath: /data/db
  volumeClaimTemplates:
    - metadata:
        name: admin-mongo-persistent-volume
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi       #FIXME CHANGE